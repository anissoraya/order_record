<%= link_to runners_path, class: 'btn btn-success' do%><i class="fa fa-arrow-left"></i> Back<%end%><br><br>
<div class="panel panel-default">
    <table class="table">
  <tr>
    <td><strong>Name:</strong></td>
    <td><%= @runner.name %></td>
  </tr>
  <tr>
    <td><strong>Phone:</strong></td>
    <td><%= @runner.phone %></td>
  </tr>
  <tr>
    <td colspan="2" style="text-align:right"><%= link_to edit_runner_path(@runner), class:'btn btn-warning' do%><i class="fa fa-pencil"></i> Edit<%end%></td>
  </tr>
  </table>
</div>
<br><br>
<!--
<legend></legend>
<h4>Tasks</h4>

<div class="table-responsive">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Order ID</th>
      <th>Customer</th>
      <th>Order Packaging (Quantity)</th>
      <th colspan="3">Manage Tasks</th>
    <tr>
  </thead>
  <tbody>
    <% @runnertasks.each do |runner_task| %>
      <tr>
        <td><%= runner_task.order.id %></td>
        <td><%= runner_task.order.customer.name %></td>
        <td><%= runner_task.package_quantity %></td>
        <td><%= link_to runner_runner_tasks_path(@runner) do%><i class="fa fa-eye"></i><%end%></td>
        <td><%= link_to edit_runner_runner_task_path(@runner, runner_task) do%><i class="fa fa-pencil"></i><%end%></td>
        <td><%= link_to [@runner, runner_task], method: :delete, data:{confirm: 'Are you sure to delete?'} do%><i class="fa fa-remove"></i><%end%></td>
      </tr>
    <%end%>
  </tbody>
</table>
</div>
<center><%= link_to 'New Task', new_runner_runner_task_path(@runner), class: 'btn btn-info'%></center>
-->
